import React, { Component } from 'react';
import pic from '../logo.svg'

class Login extends Component {
    render() {
        return (
            <div>
                <img src={pic} alt={'aaaa'}/>
            </div>
        )
    }
}

export default Login;